baseurl:        /

markdown:       redcarpet
redcarpet:
  # extensions:   ["no_intra_emphasis", "fenced_code_blocks", "autolink", "tables", "with_toc_data"]
  extensions:   ["no_intra_emphasis", "fenced_code_blocks", "autolink", "tables"]
highlighter:       pygments
paginate:       300
paginate_path:  "index/page:num"
permalink:      /posts/:title/

title:          ""

exclude:        ['_fe', 'README.md', 'base.sh', 'manager.sh', '_docker', 'roles']

gems:           ['jekyll-paginate']

host_name:      'www.liaohuqiu.com'
s_host:         '/assets/blog-img'
error_s_host:   'cimage.sinaapp.com'

author:         liaohuqiu@gmail.com 廖祜秋

# -----------------

# Multiple languages
languages:          ['en', 'cn']

# If not config, the first of languages will be the default
language_default:   "en"

# -----------------
